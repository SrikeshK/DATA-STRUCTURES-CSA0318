#include<stdio.h>
int main()
{
	int a[]={1,2,3,4,5,6,7};
	int n=sizeof(a)/sizeof(a[0]);
	int del=4,i;
	printf("Position to be Deleted : %d \n",del );
	for(i=del-1;i<n-1;i++)
	{
		a[i]=a[i+1];
	}
	printf("After Deleting : ");
	for(i=0;i<n-1;i++)
	{
		printf("%d ",a[i]);
	}
}
